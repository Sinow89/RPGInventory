#include <stdio.h>
#include "raylib.h"
#include "raymath.h"

int main(){

    SetTargetFPS(60);
    InitWindow(800, 600, "RPG - Inventory");

    while(!WindowShouldClose()){
        BeginDrawing();
        ClearBackground(BLACK);


        






        EndDrawing();
    }
    CloseWindow();
}